# One-time portrait pipeline deps (prep_photo.py + make_ascii_svg.py).
# Not needed by the daily CI workflow -- run locally when the source photo changes:
#   pip install -r scripts/requirements-portrait.txt
#   python scripts/prep_photo.py assets/source/dkIcon.png
#   python scripts/make_ascii_svg.py
Pillow>=10.0.0
numpy>=1.26.0
opencv-python-headless>=4.9.0
rembg>=2.0.50
onnxruntime>=1.17.0
